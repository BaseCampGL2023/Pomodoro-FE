import { PomodoroBadgeDirective } from './pomodoro-badge.directive';

describe('PomodoroBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new PomodoroBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
